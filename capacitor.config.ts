import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'my-next-capacitor-app',
  webDir: 'public',
  bundledWebRuntime: false
};

export default config;
